const User = require('../models/userModel');

const getUserPermissions = async(user_id) => {
    try{

    } catch(error){
        console.log(error.message);
    }
}

module.exports = {
    getUserPermissions
}